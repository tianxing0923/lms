md-input-container.md-block
  label 标题
  input(name='title', ng-model='form.title', required)
  div(ng-messages='courseForm.title.$error')
    div(ng-message='required') 请输入标题
md-input-container.md-block
  label 讲师
  input(name='lecturer', ng-model='form.lecturer', required)
  div(ng-messages='courseForm.lecturer.$error')
    div(ng-message='required') 请输入讲师
md-input-container.md-block
  label 讲师介绍
  textarea(name='lecturerIntroduction', rows='3', ng-model='form.lecturerIntroduction')
md-input-container.md-block.md-input-has-value(ng-class='{ "md-input-invalid" : courseForm.categories.$invalid && (!courseForm.categories.$pristine || dataForm.$submitted) }')
  label.md-required 分类
  div(layout='row')
    md-checkbox(ng-repeat='item in categories', ng-value='item._id', ng-checked='form.categories.indexOf(item._id) != -1', ng-click='selected(item)') {{item.name}}
    input.hide(type='number', name='categories', ng-model='form.categories.length', min='1')
  div(ng-messages='courseForm.categories.$error')
    div(ng-message='min') 请选择分类
md-input-container.md-block
  label 概述
  textarea(name='summary', rows='3', ng-model='form.summary', required)
  div(ng-messages='courseForm.summary.$error')
    div(ng-message='required') 请输入概述
md-input-container.md-block.md-input-has-value
  label.md-required 内容
  textarea(name='content', froala, ng-model='form.content', required)
  div(ng-messages='courseForm.content.$error')
    div(ng-message='required') 请输入内容